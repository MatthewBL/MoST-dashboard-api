# Optional port for the API server
PORT=4000

# Optional: override the project root used to locate requests/. Default is one level up from api/
MOST_PROJECT_ROOT=..

# Optional: override requests directory name (relative to MOST_PROJECT_ROOT)
REQUESTS_DIR=requests
