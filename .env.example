# Optional port for the API server
PORT=4000

# Optional: override the project root used to locate results/. Default is one level up from api/
MOST_PROJECT_ROOT=..

# Optional: override results directory name (relative to MOST_PROJECT_ROOT)
RESULTS_DIR=results

# SSH Tunnel Configuration
BACKEND_NODE=c03
DEPLOYMENT_PERIOD=720:00:00